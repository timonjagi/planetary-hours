import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.planethours.torus.app',
  appName: 'planetary-hours',
  webDir: 'dist'
};

export default config;
